{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the main window. Plugins are driven from Rust, so none of their permissions are declared here — the ACL is enforced only on webview-initiated calls.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
